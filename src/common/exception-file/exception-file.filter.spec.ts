import { ExceptionFileFilter } from './exception-file.filter';

describe('ExceptionFileFilter', () => {
  it('should be defined', () => {
    expect(new ExceptionFileFilter()).toBeDefined();
  });
});
